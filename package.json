{
  "name": "narrator-ts",
  "version": "0.0.0",
  "description": "A faithful TypeScript reimplementation of the Amiga narrator.device and translator.library",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "oracle:build": "bash tools/fetch-musashi.sh && make -C tools/oracle",
    "fixtures": "python3 tools/extract-devices.py"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.8",
    "@types/node": "^22.10.2"
  }
}
